spring:
  profiles:
    default: local
  application:
    name: account-service

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

  endpoint:
    health:
      show-details: always